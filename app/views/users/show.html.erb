<div class="container">
  <%= render "shared/profile" %>
  <ul id="tabs-swipe-demo" class="tabs">
    <li class="tab col s3"><a class="active" href="#test-swipe-1">投稿一覧</a></li>
    <li class="tab col s3"><a href="#test-swipe-2">いいね一覧</a></li>
  </ul>
  <div id="test-swipe-1" class="col s12">
    <div class="row">
      <%= render partial: "shared/shops", object: @shops, as: :shops %>
    </div>
    <%= paginate(@shops)%>
  </div>
  <div id="test-swipe-2" class="col s12">
    <div class="row">
      <%= render partial: "shared/shops", object: @liked_shops, as: :shops %>
    </div>
    <%= paginate(@liked_shops)%>
  </div>
</div>
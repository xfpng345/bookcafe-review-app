<%= render "shared/search"%>
  <div class="container">
    <div class="row">
      <div class="col s12">
        <%= render partial: "shared/shops", object: @shops, as: :shops %>
      </div>
    </div>
  </div>
<%= paginate(@shops)%>
